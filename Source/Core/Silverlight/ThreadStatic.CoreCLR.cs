using System;
using System.Collections.Generic;
using System.Text;
using System.Threading;

namespace PHP.CoreCLR
{
/*	class ThreadStatic<T>
	{
		private LocalDataStoreSlot slot;

		public ThreadStatic(string id) : this(id, default(T)) { }

		public ThreadStatic(string id, T value)
		{
			slot = Thread.GetNamedDataSlot(id);
			Thread.SetData(slot, value);
		}

		public T Value
		{
			get { return (T)Thread.GetData(slot); }
			set { Thread.SetData(slot, value); }
		}
	
	}*/
}
